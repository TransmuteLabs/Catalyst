./register.ts hooks: session.start, command.run{command=clear|resume}, command.run{command=catalyst-ladder}, prompt.section, tool.describe, command.describe, tool.call, agent.spawn, turn.step
./register.ts calls: $.agent.list, $.clock.every (via armFailoverFoldTimer), $.clock.now (via nowMs), $.clock.sleep (via raceDeadline), $.command.register, $.env.get (via envBundle, worldFor), $.fs.read (via readText), $.fs.write, $.model.complete (via consultBg), $.session.id (via sidFor), $.session.messages (via consultBg), $.store.delete (via sweepVerdictStore), $.store.get, $.store.keys (via sweepVerdictStore), $.store.set, $.ui.toast (via consultBg)
./register.ts env writes: nothing
./register.ts env reads: CATALYST_ROUTING_TABLE, CLAUDE_CONFIG_DIR, CLAUDE_FORM, CLAUDE_FORM_CARRIER, CLAUDE_IDLE, CLAUDE_IDLE_CARRIER, CLAUDE_JUDGE, CLAUDE_JUDGE_CARRIER, CLAUDE_JUDGE_MODEL, CLAUDE_JUDGE_PROMPT, CLAUDE_JUDGE_TIMEOUT_MS, CLAUDE_PROBES, CLAUDE_PROBES_DIR, CLAUDE_PROMPTS, HOME, PWD
