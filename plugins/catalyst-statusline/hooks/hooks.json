{
  "description": "Draws the multi-line status HUD above the prompt from pure data families (one core subscription per event, cmd/file sources run only while their element is placed), reveals each segment's details on hover, serves /statusline-mod with the six-tab pill panel and a live preview.",
  "modules": ["./statusline.ts"]
}
